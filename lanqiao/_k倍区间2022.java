import java.util.*;
import java.io.*;

public class _k倍区间2022 {
    static long mod(long divd, long divs) {
        // if ()
        return divd;
    }

    public static void main(String[] args) throws IOException {
        StreamTokenizer in = new StreamTokenizer(new BufferedReader(new InputStreamReader(System.in)));
        System.out.println(-1.0 / 3);
        System.out.println(-1 % 5);
        System.out.println(1 % -5);
        // in.nextToken();
        // int n = (int) in.nval;
        // in.nextToken();
        // int k = (int) in.nval;
        // long[] s = new long[n + 1];
        // for (int i = 1; i <= n; ++i) {
        //     in.nextToken();
        //     s[i] = s[i - 1] + (int) in.nval;
        // }
        // int[] cnts = new int[k];
        // cnts[0] = 1;
        // long cnt = 0L;
        // for (int i = 1; i <= n; ++i) {
        //     int rem = (int) (s[i] % k);
        //     if (s[i] > 0) {
        //         cnt += cnts[rem];
        //     }
        //     ++cnts[rem];
        // }
        // System.out.println(cnt);
    }
}
