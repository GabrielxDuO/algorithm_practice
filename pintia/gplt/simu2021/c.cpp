#include <cstdio>

using namespace std;

int x;

int main() {
    scanf("%d", &x);
    if (x >= 90) printf("gong xi ni kao le %d fen!\n", x);
    else printf("kao le %d fen bie xie qi!\n", x);

    return 0;
}